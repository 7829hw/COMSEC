services:
  bot:
    build:
      context: ./bot
    container_name: comsec-bot
    env_file:
      - .env
    environment:
      - TZ=Asia/Seoul
    volumes:
      - ./data:/app/data
    restart: unless-stopped
